version: '3.3'
#http://192.168.99.100:9000/ wegen Toolbox

services:
  webserver:
    build: docker/php
    ports:
      - '9000:80'
    volumes:
      - ./code:/var/www/html
      - ./fonts:/usr/share/fonts/truetype/custom
    networks:
      - sharepicgenerator
  grunt:
    build: docker/node
    volumes:
      - ./code:/var/www/html
    command: grunt
    working_dir: /var/www/html
    networks:
      - sharepicgenerator
networks:
  sharepicgenerator: