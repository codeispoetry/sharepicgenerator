$(document).ready(() => {
  $('#text').val('Social Media\nwie 1980.');
  $('#textX').val('279');
  $('#textY').val('111');
  claim.draw();
  subline.draw();
});

// eslint-disable-next-line no-unused-vars
function reset() {
  // do nothing, stay here
}
